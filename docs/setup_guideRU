Sky Candle Tracker — Руководство по установке
Это подробная инструкция по настройке трекера свечей Sky в своей Google-таблице.
⚠️ Важно:
Код в /src/Code.gs уже настроен под русские названия листов, чтобы всё работало без ошибок.
Если хотите использовать английские названия — см. блок в конце.

1. Создайте Google-таблицу

Создайте лист с названием УЧЕТ и добавьте такие столбцы в указанном порядке:
	1.	Дата
	2.	Вчера
	3.	Сегодня
	4.	Потрачено
	5.	Получено
	6.	Вообщем
	7.	Сезонные🧨
	8.	Потрачено🧨
	9.	Получено🧨
	10.	Примечание

Создайте второй лист — СТАТИСТИКА (оставьте пустым).

⸻

2. Установка скрипта
	1.	Перейдите в Extensions → Apps Script / Дополнения → Apps Script
	2.	Удалите весь код в файле Code.gs
	3.	Вставьте код из /src/Code.gs
	4.	Нажмите Сохранить

⸻

3. Настройка часового пояса

Откройте:

Project Settings → Script Timezone → GMT+5 (Asia/Almaty)

Это важно, потому что ежедневный сброс свечей в Sky привязан к этому времени.

⸻

4. Создайте триггер ежедневного добавления строки
	1.	Откройте вкладку Triggers / Триггеры
	2.	Создайте новый триггер:
	•	Функция → addDailyRowIfNeeded
	•	Тип → Time-based / По времени
	•	Частота → Каждый день
	•	Время → 13:00 (или любое подходящее)

⸻

5. Первоначальный запуск

Запустите вручную функции:
	•	recalcAll()
	•	updateStats()

Google запросит разрешения — подтвердите.

⸻

6. Готово!

После этого ваш трекер:
	•	Каждый день создаёт новую строку
	•	Автоматически пересчитывает свечи
	•	Ведёт статистику
	•	Учитывает сезонные свечи и траты
	•	Работает полностью автономно

⸻

Дополнительно

Как включить английский язык для таблицы

Если хочешь, чтобы таблица и код работали на английском:
	1.	Зайди в файл Code.gs
	2.	Найди блок переменных с названиями столбцов
	3.	Замени русские названия на английские (например “Вчера” → “Yesterday”)
	4.	Переименуй заголовки столбцов в самой таблице
